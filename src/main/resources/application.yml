
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/redshift?serverTimezone=UTC
        username: root
        password: test
        driver-class-name: com.mysql.cj.jdbc.Driver
        initialization-mode: always
    jpa:  
        show-sql: true
        generate-ddl: true
        hibernate:
            ddl-auto: update
        properties:
            dialect: org.hibernate.dialect.MySQL5Dialect
            hibernate:
                format_sql: true
